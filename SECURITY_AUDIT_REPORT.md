# تقرير الفحص الأمني الشامل لمشروع حقك (Haqak)
**التاريخ:** 25 مارس 2026  
**الحالة:** تم اكتشاف عدة ثغرات حرجة وحساسة

---

## 1. الثغرات الحرجة (Critical)

### 1.1 ثغرة تجاوز نظام الحظر (Ban Bypass Vulnerability)
**الملف:** `src/pages/CitizenDashboard.tsx` (الأسطر 142-154)  
**المشكلة:**
- فحص حالة الحظر يتم في الواجهة الأمامية (Frontend) فقط
- أي مستخدم يمكنه استدعاء الـ API مباشرة دون المرور بفحص الحظر
- لا توجد تحقق من جانب الخادم (Server-side validation)

**التأثير:** مستخدم محظور يمكنه الاستمرار في إرسال شكاوى مسيئة عبر استدعاء الـ API مباشرة

**الحل المقترح:**
- نقل فحص الحظر إلى Supabase Edge Function
- إضافة تحقق من `banned_until` قبل معالجة الشكوى

---

### 1.2 ثغرة في نظام الفلترة (Insufficient Content Filtering)
**الملف:** `supabase/functions/classify-issue/index.ts` (الأسطر 73-108)  
**المشكلة:**
- نظام الفلترة يعتمد على Gemini AI فقط دون تحقق محلي
- إذا فشل الـ API أو تم تجاوزه، سيتم قبول محتوى مسيء
- لا توجد قائمة سوداء (Blacklist) محلية للكلمات المحظورة

**التأثير:** محتوى مسيء قد يمر دون كشف

**الحل المقترح:**
- إضافة فحص محلي للكلمات المحظورة قبل استدعاء Gemini
- تطبيق Fallback logic إذا فشل الـ API

---

### 1.3 ثغرة في التحقق من CAPTCHA (Missing Server-side CAPTCHA Verification)
**الملف:** `src/components/TurnstileCaptcha.tsx`  
**المشكلة:**
- CAPTCHA موجود في الواجهة الأمامية فقط
- لا يوجد تحقق من صحة CAPTCHA في الخادم
- أي شخص يمكنه تجاوز CAPTCHA بسهولة

**التأثير:** هجمات الإغراق (Spam) والروبوتات

**الحل المقترح:**
- إضافة تحقق من CAPTCHA في Edge Function
- استدعاء Cloudflare Turnstile API للتحقق

---

## 2. الثغرات الحساسة (High)

### 2.1 ثغرة في سياسات الأمان (RLS Weakness)
**الملف:** `supabase/migrations/20260322005441_e6e6fc68-28e5-4511-bafa-84f292a59770.sql`  
**المشكلة:**
- أي مستخدم مسجل يمكنه رؤية مرفقات الشكاوى الخاصة بمستخدمين آخرين
- سياسة الأمان تسمح بـ SELECT على جميع الملفات لأي مستخدم مسجل

**التأثير:** تسرب البيانات الشخصية والملفات الحساسة

**الحل المقترح:**
- تحديد سياسة الأمان لتسمح فقط للمستخدم الذي أرسل الشكوى والنائب المعين

---

### 2.2 ضعف في نظام Rate Limiting
**الملف:** `supabase/functions/shared/rate-limiter.ts`  
**المشكلة:**
- يعتمد على Redis غير مهيأ في بيئة Edge Functions
- قد يفشل النظام عند الضغط العالي
- لا يوجد معالجة للأخطاء

**التأثير:** هجمات DDoS غير محمية

**الحل المقترح:**
- استخدام Supabase Rate Limiting أو تطبيق محلي في الدالة

---

### 2.3 عدم وجود تسجيل الأحداث (Audit Logging)
**المشكلة:**
- لا يوجد تسجيل للمحاولات الفاشلة أو المشبوهة
- لا يمكن تتبع محاولات الإساءة

**التأثير:** صعوبة الكشف عن الهجمات والتحقيق فيها

**الحل المقترح:**
- إضافة جدول `audit_logs` لتسجيل جميع المحاولات المشبوهة

---

## 3. الأخطاء والمشاكل التقنية (Medium)

### 3.1 معالجة الأخطاء الضعيفة
**الملف:** `supabase/functions/classify-issue/index.ts` (الأسطر 159-165)  
**المشكلة:**
- رسالة خطأ عامة جداً
- لا تساعد في تشخيص المشكلة

**الحل المقترح:**
- إضافة رسائل خطأ أكثر تفصيلاً

---

### 3.2 عدم التحقق من حجم الملفات
**الملف:** `src/pages/CitizenDashboard.tsx` (الأسطر 97-108)  
**المشكلة:**
- لا يوجد فحص لحجم الملفات المرفوعة
- قد يؤدي لاستهلاك مساحة التخزين

**الحل المقترح:**
- إضافة فحص لحجم الملفات قبل الرفع

---

### 3.3 عدم وجود تشفير للملفات الحساسة
**المشكلة:**
- الملفات المرفوعة لا تُشفر
- قد تكون عرضة للاختراق

**الحل المقترح:**
- تشفير الملفات قبل الرفع

---

## 4. ملخص التوصيات

| الأولوية | العدد | الإجراء |
|---------|------|--------|
| حرج | 3 | يجب إصلاحه فوراً |
| حساس | 3 | يجب إصلاحه قريباً |
| متوسط | 3 | يجب إصلاحه في التحديث القادم |

---

## 5. نتائج الفحص الأولية

✅ **تم اكتشاف:** 9 مشاكل  
✅ **الـ API المستخدم:** Gemini 2.0 Flash عبر Supabase  
✅ **حالة الفلترة:** موجودة لكن غير كافية  
✅ **الحالة العامة:** يحتاج لتحسينات أمنية عاجلة
