import{createClient}from "https://esm.sh/@supabase/supabase-js@2.49.2";type RequestPayload={submission_id?:unknown;contribution_id?:unknown;name?:unknown;email?:unknown;message?:unknown;language?:unknown;honeypot?:unknown;};type RuntimeConfig={delivery_enabled:boolean;sender_email:string;recipient_email:string;rate_limit_salt:string;worker_token:string;};type StoredFeedback={public_reference:string;delivery_status:string;};type RateLimitResult={allowed:boolean;retry_after_seconds:number;};const JSON_HEADERS={"Content-Type":"application/json; charset=utf-8"};const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const EMAIL_PATTERN=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;const MAX_BODY_BYTES=16_384;function allowedOrigins():Set<string>{return new Set(["https://haqak.org","https://www.haqak.org"]);}function corsHeaders(origin:string|null):Record<string,string>{const allowed=origin&&allowedOrigins().has(origin)?origin:"";return{"Access-Control-Allow-Origin":allowed,"Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"authorization, apikey, content-type, x-client-info","Access-Control-Max-Age":"86400",Vary:"Origin",};}function json(body:Record<string,unknown>,status:number,cors:Record<string,string>,extraHeaders:Record<string,string>={},):Response{return new Response(JSON.stringify(body),{status,headers:{...cors,...JSON_HEADERS,...extraHeaders},});}function readString(value:unknown):string{return typeof value==="string"?value.trim():"";}function escapeHtml(value:string):string{return value.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}function createReference():string{const date=new Date().toISOString().slice(0,10).replaceAll("-","");const bytes=new Uint8Array(6);crypto.getRandomValues(bytes);const suffix=Array.from(bytes).map((byte)=>byte.toString(16).padStart(2,"0")).join("").toUpperCase();return`HQK-SUP-${date}-${suffix}`;}async function hmacSha256Hex(key:string,message:string):Promise<string>{const encoder=new TextEncoder();const cryptoKey=await crypto.subtle.importKey("raw",encoder.encode(key),{name:"HMAC",hash:"SHA-256"},false,["sign"],);const signature=await crypto.subtle.sign("HMAC",cryptoKey,encoder.encode(message));return Array.from(new Uint8Array(signature)).map((byte)=>byte.toString(16).padStart(2,"0")).join("");}function extractModernSecretKey(raw:string|null):string|null{if(!raw)return null;const trimmed=raw.trim();if(trimmed.startsWith("sb_secret_"))return trimmed;try{const parsed=JSON.parse(trimmed)as unknown;const values=Array.isArray(parsed)?parsed:parsed&&typeof parsed==="object"?Object.values(parsed as Record<string,unknown>):[];for(const value of values){if(typeof value==="string"&&value.startsWith("sb_secret_"))return value;if(value&&typeof value==="object"){for(const nested of Object.values(value as Record<string,unknown>)){if(typeof nested==="string"&&nested.startsWith("sb_secret_"))return nested;}}}}catch{return null;}return null;}function getAdminKey():string{return(Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")?.trim()||Deno.env.get("SUPABASE_SECRET_KEY")?.trim()||extractModernSecretKey(Deno.env.get("SUPABASE_SECRET_KEYS"))||"");}function clientIp(req:Request):string{return(req.headers.get("CF-Connecting-IP")??req.headers.get("X-Forwarded-For")?.split(",")[0]?.trim()??"0.0.0.0");}async function getRuntimeConfig(admin:any):Promise<RuntimeConfig>{const{data,error}=await admin.rpc("get_support_feedback_runtime_config");if(error)throw new Error(`RUNTIME_CONFIG:${error.code}`);const row=Array.isArray(data)?data[0]:data;if(!row||typeof row!=="object")throw new Error("RUNTIME_CONFIG:EMPTY");const config=row as Record<string,unknown>;return{delivery_enabled:config.delivery_enabled===true,sender_email:String(config.sender_email??""),recipient_email:String(config.recipient_email??""),rate_limit_salt:String(config.rate_limit_salt??""),worker_token:String(config.worker_token??""),};}async function consumeLimit(admin:any,bucketKey:string,windowSeconds:number,maxRequests:number,):Promise<RateLimitResult>{const{data,error}=await admin.rpc("consume_support_feedback_rate_limit",{p_bucket_key:bucketKey,p_window_seconds:windowSeconds,p_max_requests:maxRequests,});if(error)throw new Error(`RATE_LIMIT_STORAGE:${error.code}`);const row=Array.isArray(data)?data[0]:data;if(!row||typeof row!=="object")throw new Error("RATE_LIMIT_STORAGE:INVALID_RESULT");const record=row as Record<string,unknown>;return{allowed:record.allowed===true,retry_after_seconds:Number(record.retry_after_seconds??0),};}function emailContent(params:{reference:string;contributionId:string|null;name:string;email:string;message:string;language:string;}):{subject:string;text:string;html:string}{const{reference,contributionId,name,email,message,language}=params;const safeName=name||"Anonymous supporter";const subject=`[${reference}] رسالة دعم جديدة | New Haqak support message`;const text=[`Reference: ${reference}`,`Name: ${safeName}`,`Email: ${email || "Not provided"}`,`Language: ${language || "Not provided"}`,`Contribution ID: ${contributionId || "Not linked"}`,"","Message:",message,].join("\n");const html=`<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><meta http-equiv="X-UA-Compatible" content="IE=edge"><title>${escapeHtml(subject)}</title></head><body style="margin:0;background-color:#f6f4f0;font-family:Arial,Helvetica,sans-serif;"><table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="width:100%;border-collapse:collapse;background-color:#f6f4f0;"><tr><td align="center" style="padding-top:28px;padding-right:16px;padding-bottom:28px;padding-left:16px;"><table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="width:100%;max-width:620px;border-collapse:collapse;background-color:#ffffff;border:1px solid #e6e0d8;"><tr><td bgcolor="#1A1A1A" style="background-color:#1A1A1A;padding-top:22px;padding-right:24px;padding-bottom:22px;padding-left:24px;"><p style="margin:0;font-family:Arial,Helvetica,sans-serif;font-size:12px;line-height:18px;color:#D4AF84;letter-spacing:1.4px;text-transform:uppercase;">Haqak Support</p><h1 style="margin-top:8px;margin-right:0;margin-bottom:0;margin-left:0;font-family:Arial,Helvetica,sans-serif;font-size:24px;line-height:32px;color:#ffffff;font-weight:700;">New support message</h1></td></tr><tr><td style="padding-top:24px;padding-right:24px;padding-bottom:24px;padding-left:24px;"><p style="margin-top:0;margin-right:0;margin-bottom:16px;margin-left:0;font-family:Arial,Helvetica,sans-serif;font-size:13px;line-height:20px;color:#6b6258;"><strong style="color:#1A1A1A;">Reference:</strong> ${escapeHtml(reference)}</p><table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="width:100%;border-collapse:collapse;"><tr><td style="padding-top:6px;padding-right:0;padding-bottom:6px;padding-left:0;font-family:Arial,Helvetica,sans-serif;font-size:14px;line-height:21px;color:#1A1A1A;"><strong>Name:</strong> ${escapeHtml(safeName)}</td></tr><tr><td style="padding-top:6px;padding-right:0;padding-bottom:6px;padding-left:0;font-family:Arial,Helvetica,sans-serif;font-size:14px;line-height:21px;color:#1A1A1A;"><strong>Email:</strong> ${escapeHtml(email || "Not provided")}</td></tr><tr><td style="padding-top:6px;padding-right:0;padding-bottom:6px;padding-left:0;font-family:Arial,Helvetica,sans-serif;font-size:14px;line-height:21px;color:#1A1A1A;"><strong>Contribution:</strong> ${escapeHtml(contributionId || "Not linked")}</td></tr></table><table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="width:100%;border-collapse:collapse;margin-top:18px;"><tr><td bgcolor="#F8F5F0" style="background-color:#F8F5F0;border:1px solid #EDE8E0;padding-top:18px;padding-right:18px;padding-bottom:18px;padding-left:18px;font-family:Arial,Helvetica,sans-serif;font-size:15px;line-height:24px;color:#2f2a25;white-space:pre-wrap;">${escapeHtml(message)}</td></tr></table></td></tr></table></td></tr></table></body></html>`;return{subject,text,html};}async function sendNotification(params:{apiKey:string;fromEmail:string;toEmail:string;reference:string;submissionId:string;contributionId:string|null;name:string;email:string;message:string;language:string;}):Promise<{sent:true;providerId:string}|{sent:false;errorCode:string}>{const content=emailContent(params);let response:Response;try{response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${params.apiKey}`,"Content-Type":"application/json","User-Agent":"Haqak-Support/2.0","Idempotency-Key":`haqak-support-${params.submissionId}`,},body:JSON.stringify({from:params.fromEmail,to:[params.toEmail],subject:content.subject,text:content.text,html:content.html,...(params.email?{reply_to:[params.email]}:{}),}),});}catch{return{sent:false,errorCode:"RESEND_NETWORK"};}let body:unknown=null;try{body=await response.json();}catch{body=null;}const providerId=body&&typeof body==="object"&&typeof(body as Record<string,unknown>).id==="string"?String((body as Record<string,unknown>).id):"";if(response.ok&&providerId)return{sent:true,providerId};if(response.status===401||response.status===403)return{sent:false,errorCode:"RESEND_AUTH_OR_DOMAIN"};if(response.status===429)return{sent:false,errorCode:"RESEND_RATE_LIMITED"};if(response.status>=500)return{sent:false,errorCode:"RESEND_UNAVAILABLE"};return{sent:false,errorCode:`RESEND_HTTP_${response.status}`};}Deno.serve(async(req:Request)=>{const origin=req.headers.get("Origin");const cors=corsHeaders(origin);if(!origin||!allowedOrigins().has(origin)){return json({accepted:false,code:"ORIGIN_NOT_ALLOWED",message:"Origin is not allowed."},403,cors);}if(req.method==="OPTIONS")return new Response("ok",{status:200,headers:cors});if(req.method!=="POST"){return json({accepted:false,code:"METHOD_NOT_ALLOWED",message:"Method not allowed."},405,cors,{Allow:"POST, OPTIONS"});}const declaredLength=Number(req.headers.get("Content-Length")??"0");if(Number.isFinite(declaredLength)&&declaredLength>MAX_BODY_BYTES){return json({accepted:false,code:"PAYLOAD_TOO_LARGE",message:"Request body is too large."},413,cors);}const supabaseUrl=Deno.env.get("SUPABASE_URL")?.trim()??"";const adminKey=getAdminKey();if(!supabaseUrl||!adminKey){return json({accepted:false,code:"SERVICE_MISCONFIGURED",message:"Support service is unavailable."},503,cors);}const admin=createClient(supabaseUrl,adminKey,{auth:{persistSession:false,autoRefreshToken:false}});let config:RuntimeConfig;try{config=await getRuntimeConfig(admin);}catch{return json({accepted:false,code:"SERVICE_MISCONFIGURED",message:"Support service is unavailable."},503,cors);}if(config.rate_limit_salt.length<64){return json({accepted:false,code:"SERVICE_MISCONFIGURED",message:"Support service is unavailable."},503,cors);}let rawBody:string;try{rawBody=await req.text();}catch{return json({accepted:false,code:"INVALID_BODY",message:"Request body could not be read."},422,cors);}if(new TextEncoder().encode(rawBody).byteLength>MAX_BODY_BYTES){return json({accepted:false,code:"PAYLOAD_TOO_LARGE",message:"Request body is too large."},413,cors);}let body:RequestPayload;try{body=JSON.parse(rawBody)as RequestPayload;}catch{return json({accepted:false,code:"INVALID_JSON",message:"Request body must be valid JSON."},422,cors);}const submissionId=readString(body.submission_id).toLowerCase();const contributionId=readString(body.contribution_id).toLowerCase();const name=readString(body.name);const email=readString(body.email).toLowerCase();const message=readString(body.message);const language=readString(body.language).slice(0,16);const honeypot=readString(body.honeypot);if(honeypot){return json({accepted:true,reference:createReference(),delivery:"delayed",duplicate:false},202,cors);}if(!UUID_PATTERN.test(submissionId)){return json({accepted:false,code:"INVALID_SUBMISSION_ID",message:"A valid submission identifier is required."},422,cors);}if(contributionId&&!UUID_PATTERN.test(contributionId)){return json({accepted:false,code:"INVALID_CONTRIBUTION_ID",message:"Contribution identifier is invalid."},422,cors);}if(name.length>100){return json({accepted:false,code:"INVALID_NAME",message:"Name is too long."},422,cors);}if(email.length>255||(email&&!EMAIL_PATTERN.test(email))){return json({accepted:false,code:"INVALID_EMAIL",message:"Email address is invalid."},422,cors);}if(message.length<10||message.length>4000){return json({accepted:false,code:"INVALID_MESSAGE",message:"Message must be between 10 and 4000 characters."},422,cors);}const{data:replay,error:replayError}=await admin.from("support_feedback_messages").select("public_reference, delivery_status").eq("submission_id",submissionId).maybeSingle();if(replayError){return json({accepted:false,code:"PERSISTENCE_UNAVAILABLE",message:"Support service is temporarily unavailable."},503,cors);}if(replay){const stored=replay as StoredFeedback;const delivery=stored.delivery_status==="sent"?"sent":"delayed";return json({accepted:true,reference:stored.public_reference,delivery,duplicate:true},delivery==="sent"?200:202,cors);}const fingerprint=await hmacSha256Hex(config.rate_limit_salt,clientIp(req));try{const shortWindow=await consumeLimit(admin,`support:10m:${fingerprint}`,600,5);const dailyWindow=await consumeLimit(admin,`support:24h:${fingerprint}`,86_400,20);const rejected=!shortWindow.allowed?shortWindow:!dailyWindow.allowed?dailyWindow:null;if(rejected){return json({accepted:false,code:"RATE_LIMITED",message:"Too many messages. Please wait before trying again."},429,cors,{"Retry-After":String(Math.max(1,rejected.retry_after_seconds))},);}}catch{return json({accepted:false,code:"RATE_LIMIT_UNAVAILABLE",message:"Support service is temporarily unavailable."},503,cors);}const reference=createReference();const queuedCode=config.delivery_enabled?null:"MAILBOX_INACTIVE";const{error:insertError}=await admin.from("support_feedback_messages").insert({public_reference:reference,submission_id:submissionId,legacy_contribution_id:contributionId||null,name:name||null,email:email||null,message,language:language||null,request_fingerprint:fingerprint,delivery_status:"pending",delivery_error_code:queuedCode,next_attempt_at:new Date().toISOString(),metadata:{source_path:"/support",queue_mode:!config.delivery_enabled},});if(insertError){if(insertError.code==="23505"){const{data:duplicate}=await admin.from("support_feedback_messages").select("public_reference, delivery_status").eq("submission_id",submissionId).maybeSingle();if(duplicate){const stored=duplicate as StoredFeedback;const delivery=stored.delivery_status==="sent"?"sent":"delayed";return json({accepted:true,reference:stored.public_reference,delivery,duplicate:true},delivery==="sent"?200:202,cors);}}return json({accepted:false,code:"PERSISTENCE_UNAVAILABLE",message:"Support service is temporarily unavailable."},503,cors);}if(!config.delivery_enabled){return json({accepted:true,reference,delivery:"delayed",duplicate:false,code:"MAILBOX_INACTIVE"},202,cors);}const resendApiKey=Deno.env.get("RESEND_API_KEY")?.trim()??"";if(!resendApiKey||!config.sender_email||!config.recipient_email){await admin.from("support_feedback_messages").update({delivery_status:"failed",delivery_error_code:"DELIVERY_CONFIG_UNAVAILABLE",next_attempt_at:new Date(Date.now()+15*60_000).toISOString(),}).eq("submission_id",submissionId);return json({accepted:true,reference,delivery:"delayed",duplicate:false,code:"NOTIFICATION_DELAYED"},202,cors);}const deliveryResult=await sendNotification({apiKey:resendApiKey,fromEmail:config.sender_email,toEmail:config.recipient_email,reference,submissionId,contributionId:contributionId||null,name,email,message,language,});if(deliveryResult.sent){await admin.from("support_feedback_messages").update({delivery_status:"sent",provider_message_id:deliveryResult.providerId,email_sent_at:new Date().toISOString(),delivery_error_code:null,attempt_count:1,last_attempt_at:new Date().toISOString(),}).eq("submission_id",submissionId);return json({accepted:true,reference,delivery:"sent",duplicate:false},200,cors);}await admin.from("support_feedback_messages").update({delivery_status:"failed",delivery_error_code:deliveryResult.errorCode,attempt_count:1,last_attempt_at:new Date().toISOString(),next_attempt_at:new Date(Date.now()+15*60_000).toISOString(),}).eq("submission_id",submissionId);return json({accepted:true,reference,delivery:"delayed",duplicate:false,code:"NOTIFICATION_DELAYED"},202,cors);});