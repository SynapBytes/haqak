project_id = "wfuofurgkswotwuzosdd"

# Note:
# The previous [auth.security] block was removed because Supabase CLI v2.84.2
# fails to parse it during Edge Functions deploy with:
# "'auth' has invalid keys: security".
# Keep auth password/security policies configured in the Supabase Dashboard
# (Authentication settings) until this key is supported by the CLI schema.
